/** Threat Map: woher blockierter Verkehr kommt. Wird in Phase 3c gefüllt. */
export default function ThreatMap(_props: { query: string; onFilter: (patch: Record<string, string>) => void }) {
  return <p class="view-placeholder">Karte folgt.</p>;
}
