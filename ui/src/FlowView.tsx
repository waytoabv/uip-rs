/** Flow View: Sankey und Zonenmatrix. Wird in Phase 3d gefüllt. */
export default function FlowView(_props: { query: string; onFilter: (patch: Record<string, string>) => void }) {
  return <p class="view-placeholder">Flussdiagramm folgt.</p>;
}
