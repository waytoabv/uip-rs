/** Dashboard: Kennzahlen, Zeitreihe, Top-Listen. Wird in Phase 3b gefüllt. */
export default function Dashboard(_props: { query: string; onFilter: (patch: Record<string, string>) => void }) {
  return <p class="view-placeholder">Dashboard folgt.</p>;
}
